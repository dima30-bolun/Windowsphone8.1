# Bolun Phone OS build and test

This repository now contains a working C vertical slice for Bolun Phone 1.0 Beta simulation. Use `./build/build.sh all` to compile the library, tests and license issuer, and `./build/build.sh test` to run the test suite.
