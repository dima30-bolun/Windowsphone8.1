#include "bolun/kernel.h"

const char *bolun_kernel_syscalls_contract(void) {
    return "Bolun Phone OS kernel subsystem syscalls: implemented by kernel_core with explicit status-returning contracts.";
}
